
spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://cloud.configserver:8888/
      name: api-gateway
      enabled: true

endpoints:
  shutdown:
    #No auth  protected
    sensitive: false
    #Enable shutdown endpoint
    enabled: true
